name: PBT e-Reporting System CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest  # Use Windows runner

    steps:
    # Checkout code
    - name: Checkout code
      uses: actions/checkout@v3

    # Verify Docker Installation
    - name: Verify Docker Installation
      run: docker --version

    # Debug: Check if Secrets are Passed Correctly
    - name: Check Secrets
      env:
        USERNAME: ${{ secrets.mizn08 }}
        PASSWORD: ${{ secrets.Ahmadmohdali }}
      run: |
        if [ -z "$mizn08" ]; then echo "❌ mizn08 is missing!"; exit 1; fi
        if [ -z "$Ahmadmohdali" ]; then echo "❌ Ahmadmohdali is missing!"; exit 1; fi
        echo "✅ Secrets are set correctly."
      shell: bash

    # Log in to Docker Hub Securely
    - name: Log in to Docker Hub
      run: echo "${{ secrets.Ahmadmohdali }}" | docker login -u "${{ secrets.mizan08 }}" --password-stdin
      shell: bash

    # Build and Push Docker Image
    - name: Build and Push Docker Image
      run: |
        docker build -t my-html-app .
        docker tag my-html-app mizn08/my-html-app:latest
        docker push mizn08/my-html-app:latest
      shell: bash
